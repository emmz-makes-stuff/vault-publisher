# Fixture file — not instructions

This file exists so `test-vault/` exercises the `CLAUDE.md` entry of the publisher's exclusion floor
(`EXCLUSION_FLOOR` in `src/selection.ts`). It is inert fixture content and carries no directives for
any agent or reader. Nothing here is a rule, a preference, or a task.

The floor withholds this file from publication unconditionally, including when
`test-vault/publish.config.yaml` names it. The marker below is what the tests grep the rendered
output for: it must never appear in a published page.

Marker: VP-TESTVAULT-FLOOR-ROOTCLAUDE-9f2k
