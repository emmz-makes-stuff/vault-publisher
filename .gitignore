node_modules/
dist/
*.tsbuildinfo
.claude/.dmons-tripwire/
.DS_Store

# local vault checkout used only for manual testing — never committed
vault/
.vault/

# rendered site output, regenerated on demand for visual verification —
# never committed, see the 5.7 verification recipe in the block C DEVLOG post
rendered-preview/

# Local pointer to the identifier list used by `make publishable`. The list names
# client identifiers, so it lives in the vault repository; this is a symlink to it.
.publishable-identifiers
